{
  "name": "pos-monorepo",
  "private": true,
  "scripts": {
    "backend:dev": "gradlew.bat bootRun",
    "backend:test": "gradlew.bat test",
    "frontend:install": "npm --prefix frontend install",
    "frontend:dev": "npm --prefix frontend run dev",
    "frontend:build": "npm --prefix frontend run build"
  }
}
