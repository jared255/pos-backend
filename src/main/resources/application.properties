spring.application.name=POS Backend API
# Base de datos local (SQLite)
db.local.url=jdbc:sqlite:./data/pos_local.db

# Base de datos remota (Supabase/PostgreSQL)
# Reemplaza estos valores localmente o usa variables de entorno:
# DB_REMOTE_URL, DB_REMOTE_USER y DB_REMOTE_PASS.
db.remote.url=jdbc:postgresql://aws-1-us-west-2.pooler.supabase.com:6543/postgres?sslmode=require
db.remote.user=postgres.qdsajugzhswkhubeasnu
db.remote.pass=YOUR-SUPABASE-DB-PASSWORD

# Pool de conexiones
db.pool.max=10
